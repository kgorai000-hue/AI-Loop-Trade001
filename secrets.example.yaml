# Copy to secrets.yaml (gitignored) -- never commit real credentials.
# Environment variables MT5_LOGIN / MT5_PASSWORD / MT5_SERVER override this file.

mt5:
  login: 0
  password: ""
  # server: "FxPro-Demo"   # optional override of config.yaml
