# Cross-platform runtime + test deps (pinned).
# MetaTrader5 is Windows-only -- install via requirements-windows.txt on the VPS.
numpy==2.5.1
pandas==3.0.3
scipy==1.18.0
PyYAML==6.0.3
anthropic==0.117.0
pytest==9.1.1
